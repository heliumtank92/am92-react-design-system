import React, { PureComponent } from 'react'
import Icon from '@mui/material/Icon'

export default class DsRemixIcon extends PureComponent {
  render () {
    return (
      <Icon baseClassName='' {...this.props} />
    )
  }
}
