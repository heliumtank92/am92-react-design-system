export default dsRules;
declare namespace dsRules {
    const headerMobileHeight: string;
    const headerDesktopHeight: string;
    const appBarMobileMinHeight: string;
    const stepperConnectorMinHeight: string;
    const searchbarMinWidth: string;
    const searchbarHeight: string;
    const drawerWidth: string;
    const drawerMiniWidth: string;
    const dialogXsMaxWidth: string;
    const bottomStaggerWorkingAreaHeight: string;
    const avatarSSize: string;
    const avatarMSize: string;
    const avatarLSize: string;
    const avatarXLSize: string;
    const avatarXXLSize: string;
    const avatar3XLSize: string;
    const formHelperTextMinHeight: string;
}
