export default breakpoints;
declare namespace breakpoints {
    const keys: string[];
    namespace values {
        const xs: number;
        const sm: number;
        const md: number;
        const lg: number;
        const xl: number;
    }
}
